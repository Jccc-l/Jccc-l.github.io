body {
    background:url(/images/background.jpg);
    background-repeat: no-repeat;
    background-attachment: fixed; //是否滚动，fixed固定，scroll滚动，local背景相对元素本身固定，而不是随着内容滚动
    background-size: cover;      //填充
    background-position: center;
}

//文章背板的颜色rgb
.main-inner > .sub-menu, .main-inner > .post-block, .main-inner > .tabs-comment, .main-inner > .comments, .main-inner > .pagination{
    background: #f5f5f56;      //此处使用十六进制颜色代码，         也可以使用rgba进行调色，
	opacity: 0.8; //实际效果为白色透明色底板              rgba的第四参数即为透明度
}
body{               //修改主体字体颜色
  color: #000;      //纯黑
}
.posts-expand .post-title-link {    //标题颜色
    color: #222;                //首页文章标题颜色， （默认为灰辨识度不高）
}
.posts-expand .post-meta-container {    //标题下的日期颜色
    color: #62a7b6;             //此处修改为红色，可自行调用rgb调色
}

//侧边框的透明度设置
.sidebar {
  opacity: 0.9;
}

//菜单栏的调色
.header-inner {     
  background: rgba(255,255,255,0.7);
}
//搜索框透明
.popup {        
  opacity: 0.5;
}

.main-inner {
    background-color: rgba(255, 255, 255, 0);  //主体背景透明
    padding: 0px 40px 40px 40px;  //调整组件位置
}

// 底部字体颜色
.footer-inner {color: #555555;}

.table-container {
  overflow: auto;
  opacity: 1;
}
